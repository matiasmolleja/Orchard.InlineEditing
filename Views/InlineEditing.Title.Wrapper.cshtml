@using Orchard.ContentManagement;
@using Orchard.Core.Contents;
@using Orchard.Core.Title.Models;
@using Newtonsoft.Json;

@{
    ContentItem contentItem = (ContentItem)Model.ContentItem;
    TitlePart titlePart = contentItem.As<TitlePart>();
    Layout.Title = Model.Title;
}


@if (AuthorizedFor(Permissions.EditContent) && Model.ContentItem.Id > 0)
{
    using (Script.Foot())
    {
        <script type="text/javascript">
            var tp =  createTitlePart(@contentItem.Id, '@titlePart.Title', 'titlePart');
            IEPageVM.parts().push(tp);
        </script>
    };

    @Display.ActionControls(contentItemId: @contentItem.Id)

    <h1 class="mce_TitlePart_@contentItem.Id" id="mce_TitlePart_@contentItem.Id">
        @Model.Title
    </h1>


}
else
{
    <h1>@Model.Title</h1>
}